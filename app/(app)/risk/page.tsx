export default function RiskPage() {
  return (
    <div>
      <h1 className="font-display text-3xl font-semibold text-ink">
        Workload risk
      </h1>
      <p className="mt-1 text-sm font-semibold text-ink-2">
        Phase 0 placeholder — risk score and warnings land in Phase 8.
      </p>
    </div>
  );
}
